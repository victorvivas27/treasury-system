{
  "rules": {
    "type-enum": [2, "always", ["feat", "fix", "docs", "style", "refactor", "test", "chore", "config","ci"]],
    "header-max-length": [2, "always", 150],
    "header-min-length": [2, "always", 5],
    "scope-case": [0, "always", "lower-case"]
  }
}
