spring:
  application:
    name: tesoreria-app
  profiles:
      active: ${APP_PROFILE:dev}

server:
      port: 5055
      servlet:
        context-path: /tesoreria

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:http://localhost:5173,http://127.0.0.1:5173}
