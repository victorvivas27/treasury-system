info:
  name: 04-Delete User
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{Url_Base}}/users/{{createdUserId}}"
  headers:
    - name: Authorization
      value: "Bearer {{userToken}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status should be 204", function () {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
