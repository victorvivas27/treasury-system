info:
  name: 02-Get Invalid ID Format
  type: http
  seq: 2
  tags:
    - alumno

http:
  method: GET
  url: "{{Url_Base}}/alumnos/{{idNumber}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status should be 400", function () {
          expect(res.getStatus()).to.equal(400);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
