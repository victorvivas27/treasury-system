info:
  name: 08-Get Familia all
  type: http
  seq: 9

http:
  method: GET
  url: "{{Url_Base}}/familias/1/detalle"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
