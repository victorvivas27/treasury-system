info:
  name: 10-Telefono With Leading Plus
  type: http
  seq: 10
  tags:
    - apoderados

http:
  method: POST
  url: "{{Url_Base}}/apoderados"
  body:
    type: json
    data: |-
      {
        "nombre":"Victor Vivas",
        "email":"victor@gmail.com",
        "telefono":"+54987654321"
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        const baseUrl = bru.getEnvVar('Url_Base');

        try {
          const listar = await bru.sendRequest({
            url: `${baseUrl}/apoderados?size=100`,
            method: 'GET',
            headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${bru.getEnvVar('adminToken')}` }
          });

          const apoderados = listar.data && listar.data.content ? listar.data.content : null;
          
          if (apoderados && apoderados.length > 0) {
            for (const ap of apoderados) {
              if (ap.codigo) {
                await bru.sendRequest({
                  url: `${baseUrl}/apoderados/${ap.codigo}`,
                  method: 'DELETE',
                  headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${bru.getEnvVar('adminToken')}` }
                });
              }
            }
          }
        } catch (e) {
          // Silenciado
        }
    - type: after-response
      code: |-
        // Post Request
        const response = res.getBody();
        const apoderadoId = response.codigo;
        // Obtener variable de entorno
        const baseUrl = bru.getEnvVar('Url_Base');
        try {
            await bru.sendRequest({
              url: `${baseUrl}/apoderados/${codigo}`,
              method: 'DELETE',
              headers: { Authorization: `Bearer ${bru.getEnvVar('adminToken')}` }
            });
          } catch(error) {

        }
    - type: tests
      code: |-
        const response = res.getBody();
        test("Deberia crear apoderado con telefono que tiene +", function() {
          expect(res.getStatus()).to.equal(201);
          expect(response.telefono).to.equal("+54987654321");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
