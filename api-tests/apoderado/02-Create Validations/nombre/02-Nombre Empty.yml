info:
  name: 02-Nombre Empty
  type: http
  seq: 2

http:
  method: POST
  url: "{{Url_Base}}/apoderados"
  body:
    type: json
    data: |-
      {
        "email": "victor@example.com",
        "telefono": "987654321"
      }
  auth: inherit

runtime:
  variables:
    - name: fields_present
      value: nombre
  scripts:
    - type: tests
      code: |-
        // //El estado debería ser 400
        // test("Status should be 400", function() {
        //   expect(res.status).to.equal(400);
        // });
        // //El código de error debería ser ERROR_VALIDACION
        // test("Error code should be ERROR_VALIDACION", function() {
        //   expect(res.body.code).to.equal("ERROR_VALIDACION");
        // });
        // //Solo se debe informar el campo nombre faltante
        // test("Should only report missing name field", function() {
        //   expect(res.body.errors).to.have.property("nombre");
        //   expect(res.body.errors).to.not.have.property("email");
        //   expect(res.body.errors).to.not.have.property("telefono");
        // });


        //El mensaje de error para el nombre debe ser correcto.
        test("Error message for name should be correct", function() {
          expect(res.body.errors.nombre).to.equal("El nombre no puede estar vacío");
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
