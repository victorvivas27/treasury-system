info:
  name: 14-Delete Apoderado By ID
  type: http
  seq: 14

http:
  method: DELETE
  url: "{{Url_Base}}/apoderados/{{apoderadoId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status should be 204", function () {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
