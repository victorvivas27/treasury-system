info:
  name: 07-Get Deleted Apoderado By ID
  type: http
  seq: 7
  tags:
    - apoderados

http:
  method: GET
  url: "{{Url_Base}}/apoderados/{{codigo}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status should be 404", function () {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
