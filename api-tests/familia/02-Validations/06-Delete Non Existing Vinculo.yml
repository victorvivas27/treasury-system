info:
  name: 06-Delete Non Existing Vinculo
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{Url_Base}}/familias/alumno-apoderado/999999"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status should be 404", function () {
          expect(res.getStatus()).to.equal(404);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
