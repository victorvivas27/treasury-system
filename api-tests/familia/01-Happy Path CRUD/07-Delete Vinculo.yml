info:
  name: 07-Delete Vinculo
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{Url_Base}}/familias/alumno-apoderado/{{familiaVinculoId}}"
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("Status should be 204", function () {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
